profile: dev

config:
  serverPort: "8080"

replicas: 1
revisionHistoryLimit: 3

# ------------------------
# HPA (Auto-Scaling)
# ------------------------
hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 10
  targetCPU: 50

# ------------------------
# Image
# ------------------------
image:
  repository: 900808296075.dkr.ecr.ap-northeast-2.amazonaws.com/goorm-cart
  tag: "8-6c165693"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ecr-secret

# ------------------------
# Service
# ------------------------
service:
  port: 80
  targetPort: 8080
  type: ClusterIP

redis:
  host: master.courm-redis-prod.epf9hv.apn2.cache.amazonaws.com
  port: 6379
  username: "app-user"
  password: "NewStrongPass123!"
  sslEnabled: true

# ------------------------
# Internal Service URLs
# ------------------------
product:
  serviceUrl: http://product-service-dev-courm-app-active




# ------------------------
# Kafka
# ------------------------
kafka:
  bootstrapServers: courm-kafka.courm-kafka.svc.cluster.local:9092
  consumerGroupId: cart-service-group
  orderTopic: order-events
  paymentTopic: payment-events
  productTopic: product-events

# ------------------------
# Cognito
# ------------------------
cognito:
  jwkSetUri: https://cognito-idp.ap-northeast-2.amazonaws.com/ap-northeast-2_HmqfrWP3T/.well-known/jwks.json
  awsJwkSetUri: https://cognito-idp.ap-northeast-2.amazonaws.com/ap-northeast-2_HmqfrWP3T/.well-known/jwks.json

# ------------------------
# Gateway API
# ------------------------
route:
  enabled: true
  gatewayName: prod-gw
  gatewayNamespace: istio-system
  path: /api/v2/cart
  pathType: PathPrefix
  weight: 1
  hostnames: []

# ------------------------
# Argo Rollouts
# ------------------------
strategy:
  type: blueGreen
  blueGreen:
    autoPromotionEnabled: true
    scaleDownDelaySeconds: 300

# ------------------------
# Resources (Guaranteed QoS: requests = limits → Evict 방지)
# ------------------------
resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "512Mi"
    cpu: "500m"

# ------------------------
# Prometheus
# ------------------------
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    additionalLabels:
      release: kube-prometheus-stack

# ------------------------
# OpenTelemetry (Tempo)
# ------------------------
otel:
  enabled: true
  serviceName: cart-service
  otlpEndpoint: "http://loki-stack.logging.svc.cluster.local:4318" # Assuming we will deploy collector there or use simpler setup
  otlpProtocol: "http/protobuf"
  tracesExporter: "otlp"
  metricsExporter: "none"
  logsExporter: "none"
  javaAgent:
    jarPath: "/otel/opentelemetry-javaagent.jar"

javaToolOptions: >-
  -javaagent:/otel/opentelemetry-javaagent.jar
  -Dotel.resource.attributes=service.name=cart-service
  -Dotel.exporter.otlp.endpoint=http://loki-stack.logging.svc.cluster.local:4318
  -Dspring.kafka.bootstrap-servers=courm-kafka.courm-kafka.svc.cluster.local:9092
  -XX:+UseContainerSupport
  -XX:MaxRAMPercentage=75.0
  -XX:+ExitOnOutOfMemoryError



logLevel: INFO
